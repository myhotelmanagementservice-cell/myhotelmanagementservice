TASK: You have permission to FIX all issues found in your previous 
audit report, EXCEPT item #7 (build.js/obfuscation pipeline mismatch) 
— skip that one entirely, it will be handled separately later.

FIX THESE ITEMS (in this order):

1. Guest service request route (item 1F): Change the frontend call in 
   public/js/guest-hub.js to use the existing /api/requests endpoint 
   instead of the non-existent /api/services/request. Match the exact 
   request body format that /api/requests already expects (check 
   server/server.js or server/routes/requests.js for the expected 
   fields). Do NOT create a new backend route — reuse the existing one.

2. Sensitive/debug logging (item #2): Remove or sanitize every console.log/
   console.error statement you listed that exposes admin emails, hotel 
   IDs, user existence, password-correctness, or database IDs — in 
   server/server.js, server/controllers/adminController.js, 
   server/controllers/superAdminController.js, 
   server/routes/superAdminRoutes.js, and public/script.js line 5316. 
   Replace with safe generic event logs (no raw emails, no raw hotel 
   IDs, no raw DB IDs) where logging is still needed for debugging.

3. Five super-admin routes (items 1A-1E): Add the missing backend 
   routes (A/B testing CRUD, hotel export, revenue export, audit 
   export, hotel PDF export) behind super-admin authentication, 
   following the pattern of similar existing routes in this codebase 
   (consistent {success, data, error} response format, tenant/access 
   validation, no unrestricted data exposure).

4. Process crash handling (item #9): Update the uncaughtException and 
   unhandledRejection handlers in server/server.js to log safely and 
   perform a graceful shutdown (process.exit after cleanup) instead of 
   continuing to run in a potentially corrupted state.

5. MongoDB indexes (item #8): Add indexes ONLY for the specific field 
   combinations you already identified as necessary (hotel_id+guest_id, 
   hotel_id+created_at, hotel_id+status, city+hotelId+updatedAt, 
   isDeleted+createdAt) on the collections that are missing from the 
   startup indexer list.

STRICT RULES — DO NOT VIOLATE THESE:
- Do NOT add any new guest-facing feature, module, or UI element — 
  only fix what's broken.
- Do NOT change any existing UI design, layout, or visual styling.
- Do NOT touch public/index.html, public/style.css, or public/script.js 
  directly if you are editing frontend logic — instead edit the 
  readable source files in public/source-backup/ (index.html.backup, 
  style.css.backup, script.js.backup) and then copy the updated file(s) 
  over to the public/ folder yourself as part of your fix (cp command), 
  since these are the files actually served live.
- Do NOT touch inaya-hotel/public/admin.html or 
  inaya-hotel/public/super-admin.html's protected/obfuscated content — 
  if fixes are needed there, edit 
  public/source-backup/admin.html.backup or 
  public/source-backup/super-admin.html.backup instead, then copy over.
- Do NOT modify manifest.json, icons, or service worker in this task — 
  that will be handled separately.
- Do NOT touch build.js at all.
- After making all fixes, start the local server (node server/server.js) 
  and verify it starts without errors, then stop it.
- Do NOT run git add, git commit, or git push — stop after local 
  verification and report a full summary of every change you made 
  (file names, what changed) so it can be reviewed before deployment.